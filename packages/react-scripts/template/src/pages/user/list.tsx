import { BaseComponent } from 'components/base';
import * as React from 'react';

export default class UserListPage extends BaseComponent {
  render() {
    return (
      <div>List</div>
    );
  }
}