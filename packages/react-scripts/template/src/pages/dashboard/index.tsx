import { BaseComponent } from 'components/base';
import React from 'react';

export default class DashboardIndexPage extends BaseComponent {
  render() {
    return (
      <div>
        Olá
      </div>
    );
  }
}